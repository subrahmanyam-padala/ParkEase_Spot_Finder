# Copy this to .env.production before local production build.
# For Azure Static Web Apps, set this as a repository/environment variable at build time.
# Example:
# REACT_APP_API_BASE_URL=https://parkease-backend.azurewebsites.net/api
REACT_APP_API_BASE_URL=
