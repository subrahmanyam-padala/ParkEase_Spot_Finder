 spring.application.name=ParkEase-backend

# 1. Database Connection Setup
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/infosys_parkease?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 3. Server Port
server.port=${PORT:8080}

# 4. Email Settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587

spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}

#spring.mail.username=parkease.mall.official@gmail.com
#spring.mail.password=ztscujbjxwepoglw

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 5. JWT Configuration
jwt.secret=${JWT_SECRET:ParkEaseSecretKey2026ParkEaseSecretKey2026ParkEaseKey}
jwt.expiration=${JWT_EXPIRATION:86400000}


# 6. Payment Gateway
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SQH1x0MJgvvfIh}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:oOm4FeksjHQV1KP8VkwVYE39}

# 7. Google Drive Settings
google.drive.credentials-file=${GOOGLE_DRIVE_CREDENTIALS_FILE:credentials.json}
google.drive.tokens-directory=${GOOGLE_DRIVE_TOKENS_DIR:tokens}
google.drive.folder-id=${GOOGLE_DRIVE_FOLDER_ID:}

# 8. CORS
app.cors.allowed-origin-patterns=${APP_CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:*,https://localhost:*,http://10.*:*,https://10.*:*,http://192.168.*:*,https://192.168.*:*}

logging.level.org.springframework.mail=${LOG_LEVEL_MAIL:INFO}
logging.level.org.springframework.web=${LOG_LEVEL_WEB:INFO}